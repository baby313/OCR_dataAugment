#include <string>
#include <vector>
#include <opencv2/opencv.hpp>

using namespace std;

#include "common/common.h"

class Layout
{
public:
    Layout(string strLocate, string strTemplate);
    ~Layout();
public:
private:

};
